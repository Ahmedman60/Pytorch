# Creating the Network
import torch
import torch.nn as nn
import matplotlib.pyplot as plt
from utils import all_letters, all_categories
from utils import load_data, load_random_example, letterToTensor, lineToTensor
